<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LANBooru (Lite Mode)</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: #1a1a2e;
      color: #e0e0e0;
      min-height: 100vh;
    }
    a { color: #6eb5ff; text-decoration: none; }
    a:hover { text-decoration: underline; }
    .header {
      background: #16213e;
      padding: 12px 16px;
      border-bottom: 1px solid #334;
      display: flex;
      align-items: center;
      gap: 16px;
      flex-wrap: wrap;
    }
    .header h1 { font-size: 1.25rem; color: #fff; }
    .header h1 a { color: inherit; }
    .search-form { display: flex; gap: 8px; flex: 1; min-width: 200px; max-width: 400px; }
    .search-form input {
      flex: 1;
      padding: 6px 10px;
      border: 1px solid #445;
      border-radius: 4px;
      background: #1a1a2e;
      color: #e0e0e0;
      font-size: 14px;
    }
    .search-form button {
      padding: 6px 14px;
      border: none;
      border-radius: 4px;
      background: #4a9eff;
      color: #fff;
      cursor: pointer;
      font-size: 14px;
    }
    .search-form button:hover { background: #3a8eef; }
    .container { padding: 16px; max-width: 1400px; margin: 0 auto; }
  </style>
</head>
<body>
  <header class="header">
    <h1><a href="/lite/">LANBooru (Lite Mode)</a></h1>
    <form class="search-form" action="/lite/" method="get">
      <input type="text" name="q" placeholder="Search tags..." value="<%= typeof query !== 'undefined' ? query : '' %>">
      <button type="submit">Search</button>
    </form>
  </header>
  <main class="container">
